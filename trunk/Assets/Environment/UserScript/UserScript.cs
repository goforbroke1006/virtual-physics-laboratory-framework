using UnityEngine;

public class UserScript : MonoBehaviour
{
    public string Name = "";
    public string Code = "";

    // Use this for initialization
    void Start()
    {
        /*Members = 
            gameObject.transform.
            parent.gameObject.transform.transform.
            parent.gameObject.
            GetComponentInChildren<PhysObjectsManager>().
            GetComponentsInChildren<PhysObject>();*/
    }

    // Update is called once per frame
    void Update()
    {
        //
    }
}
